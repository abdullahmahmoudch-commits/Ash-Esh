{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ash-esh/server && npm install"
  },
  "deploy": {
    "startCommand": "cd ash-esh/server && node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
